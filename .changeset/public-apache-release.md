---
"@mimu-sh/ownable-core": patch
"@mimu-sh/ownable-payments": patch
---

First public release. Relicensed from UNLICENSED to Apache-2.0 and published to the public npm
registry with provenance, instead of privately to GitHub Packages.

No runtime behaviour changes. Package metadata gains `description`, `keywords`, `homepage`,
`bugs`, and `author`, and the packed tarballs now include `LICENSE` and `NOTICE`.
